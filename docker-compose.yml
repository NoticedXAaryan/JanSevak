# docker-compose.yml
# Runs JanSeva Bot only.
# Connects to an external managed PostgreSQL instance.
# Usage: Set environment variables in Dokploy, then deploy.

services:
  bot:
    build: .
    container_name: janseva-bot
    environment:
      # --- Critical Connections ---
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:?Set TELEGRAM_BOT_TOKEN in Dokploy or .env}
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL (asyncpg) to your external Postgres}
      DATABASE_URL_SYNC: ${DATABASE_URL_SYNC:?Set DATABASE_URL_SYNC to your external Postgres}
      
      # --- API Keys ---
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      
      # --- Provider Config ---
      LLM_PROVIDER: ${LLM_PROVIDER:-gemini}
      LLM_MODEL: ${LLM_MODEL:-gemini-2.0-flash}
      EMBEDDING_PROVIDER: ${EMBEDDING_PROVIDER:-gemini}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-models/embedding-001}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://localhost:11434}
      
      # --- Security & Settings ---
      ADMIN_JWT_SECRET: ${ADMIN_JWT_SECRET:-change-this-to-a-random-64-char-string}
      REPORT_ENCRYPTION_KEY: ${REPORT_ENCRYPTION_KEY:-change-this-to-a-fernet-key}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-change-this-in-production}
      ENV: ${ENV:-production}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      
      # --- Startup Behaviors ---
      RUN_MIGRATIONS: ${RUN_MIGRATIONS:-true}
      SEED_KNOWLEDGE_BASE: ${SEED_KNOWLEDGE_BASE:-true}
      
    volumes:
      # Persistent storage for ChromaDB (local vector store)
      - chromadata:/app/.chromadb
    networks:
      - dokploy-network

volumes:
  chromadata:

networks:
  dokploy-network:
    external: true
